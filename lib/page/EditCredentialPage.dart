import 'package:flutter/material.dart';

class EditCredentialPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Create and edit'),
      ),
      body: Scaffold(
        body: Center(
            child: ListView(
              padding: const EdgeInsets.all(16.0)
              children: [
                  
              ],
            )
        ),
      ),
    );
  }
}
